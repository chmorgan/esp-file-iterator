description: Iterator for a list of files in a directory, could be used to track an audio or image playlist
url: https://github.com/chmorgan/esp-file-iterator
dependencies:
  ## Required IDF version
  idf:
    version: ">=4.1.0"
